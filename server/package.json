{
  "name": "server",
  "version": "0.0.0",
  "private": "true",
  "type": "module",
  "scripts": {
    "build": "tsc --watch",
    "serve": "node --watch --env-file=.env dist/app.js",
    "start": "npm run build & npm run serve",
    "test": "jest --watch"
  },
  "dependencies": {
    "@prisma/client": "^5.19.1",
    "express": "^5.0.0"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/express": "^4.17.21",
    "@types/supertest": "^6.0.2",
    "jest": "^29.7.0",
    "prisma": "^5.19.1",
    "supertest": "^7.0.0",
    "ts-jest": "^29.2.5"
  }
}
