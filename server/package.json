{
  "name": "server",
  "version": "0.0.0",
  "private": "true",
  "type": "module",
  "dependencies": {
    "express": "^4.21.0"
  },
  "scripts": {
    "build": "tsc --watch",
    "server": "node --watch --env-file=.env dist/app.js",
    "start": "npm run build & npm run server"
  },
  "devDependencies": {
    "@types/express": "^4.17.21"
  }
}
